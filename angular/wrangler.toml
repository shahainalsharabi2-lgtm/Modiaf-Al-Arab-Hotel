name = "hotel-management"
compatibility_date = "2024-06-01"
main = "worker.ts"

# إبقاء Render API نشطاً — يقلل الحاجة لفتح Swagger يدوياً
[triggers]
crons = ["*/14 * * * *"]

[assets]
directory = "./dist/Hotel/browser"
binding = "ASSETS"
not_found_handling = "single-page-application"
